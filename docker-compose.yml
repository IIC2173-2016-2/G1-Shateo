version: '2'
services:
  mongo:
    image: mongo
    ports:
    - "27017"
  web:
    image: danieldent/meteor
    volumes:
      - ./:/app
    ports:
      - "3000:80"
    environment:
      - MONGO_URL=mongodb://mongo:27017/shateo
      - ROOT_URL=http://localhost:3000
    links:
      - mongo
